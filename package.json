{
  "name": "get_your_internship",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "install": "cd frontend && npm install --no-save && cd ../backend && npm install --no-save && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}